# دليل المعارف البرمجية والخبرات المكتسبة (05_KNOWLEDGE.md)

يوثق هذا الملف أهم المعارف والخبرات التقنية والحلول الهندسية التي تم تعلمها وتطبيقها خلال تطوير وربط منصة **Spoken English** بقواعد البيانات سحابياً.

---

## 1. تجميع الاتصالات لقواعد البيانات في البيئات السحابية (Connection Pooling in Serverless)
* **المفهوم:** في البيئات التقليدية، يفتح السيرفر اتصالاً مستمراً بقاعدة البيانات. أما في سيرفرات Netlify السحابية (Serverless Functions)، يتم تشغيل خوادم Node.js مؤقتة مع كل طلب وإغلاقها فورياً.
* **المشكلة:** كثرة فتح وإغلاق الاتصالات المباشرة تؤدي لتجاوز حد الاتصالات المسموح بها في NeonDB وحدوث أخطاء `Connection Timeout`.
* **الدرس المكتسب:** استخدام وسيط تجميع الاتصال (Connection Pooler) المتاح سحابياً عبر إضافة `-pooler` لعنوان الخادم. يقوم هذا الوسيط بجمع مئات الاتصالات المؤقتة وإعادة توزيعها بذكاء في قنوات ثابتة ومستقرة، مما يضمن كفاءة واستقرار قاعدة البيانات.

---

## 2. استعلامات الدمج والتحديث الذري (Atomic PostgreSQL Upsert)
* **المفهوم:** عند قيام المعلم بتسجيل الحضور، يجب حماية النظام من إدخال سجلين لنفس الفصل في نفس اليوم، مع إتاحة تعديل الكشوفات القديمة.
* **الدرس المكتسب:** بدلاً من إجراء فحصين منفصلين بقاعدة البيانات (مما يتسبب في تأخير وضغط)، تم استخدام استعلام الـ `UPSERT` المتقدم في لغة SQL لحفظ البيانات وتحديثها في عملية ذرية واحدة:
  ```sql
  INSERT INTO attendance_logs (session_date, class_level, records, stats)
  VALUES ($1, $2, $3, $4)
  ON CONFLICT (session_date, class_level)
  DO UPDATE SET records = EXCLUDED.records, stats = EXCLUDED.stats;
  ```
  هذا يضمن كفاءة معالجة البيانات بنسبة 100% ويمنع حدوث أخطاء المفاتيح المكررة.

---

## 3. محاكاة شريط التنقل للهواتف بالـ CSS فقط (Pure CSS Bottom Tab Bar)
* **المفهوم:** كانت واجهة النظام تظهر مشوهة على الشاشات الصغيرة بسبب عرض القائمة الجانبية (Sidebar) الثابتة.
* **الدرس المكتسب:** بدلاً من تعديل هيكلية كود الـ HTML أو كتابة أسطر جافا سكريبت جديدة للتحكم بالقائمة، تم الاكتفاء بتعديل خصائص الـ CSS في الشاشات التي يقل عرضها عن 768 بكسل:
  - تحويل السايدبار إلى وضع `position: fixed` بالأسفل وتغيير اتجاه العناصر ليكون أفقياً (`flex-direction: row`).
  - تحويل أزرار التنقل وفوتر تسجيل الخروج لتظهر على هيئة أيقونات مكدسة طولياً (شريط ملاحة سفلي ذكي).
  - هذا يحافظ على سلامة الكود البرمجي البرمجي ويوفر تجربة تصفح هاتفية طبيعية وخفيفة.

---

## 4. حقن مسارات المكتبات البرمجية بالـ Node.js (Module Path Hijacking)
* **المشكلة:** واجهنا فشل تشغيل سكربت الإعداد التلقائي لقاعدة البيانات `init_db.js` بسبب خطأ `Cannot find module 'pg'` نظراً لموقع السكربت المؤقت خارج مجلد المشروع.
* **الدرس المكتسب:** تبحث لغة Node.js في المجلدات الأبوية للملف الجاري تشغيله عن مجلد `node_modules`. لحل هذه المشكلة برمجياً، تم حقن مسار المجلد الرئيسي للمشروع مباشرة في مصفوفة البحث قبل عملية الاستيراد:
  ```javascript
  module.paths.push('c:\\Users\\hk\\Desktop\\Attendance_App_Project\\node_modules');
  const { Client } = require('pg');
  ```
  تتيح هذه الطريقة تشغيل أي سكربت خدمي من أي مكان بنجاح والاتصال بالمكتبات المحلية للمشروع.

---

## 5. الحماية ضد أخطاء الـ Null في واجهات الـ SPA
* **المفهوم:** في تطبيقات الصفحة الواحدة (SPA)، يتم تحميل الكود البرمجي بالكامل للتحكم بشاشات متعددة.
* **المشكلة:** محاولة قراءة أو كتابة نص لعنصر غير معروض بالصفحة الحالية يرمي خطأ `TypeError: Cannot set properties of null` ويوقف تشغيل محرك الجافا سكريبت بالكامل.
* **الدرس المكتسب:** ضرورة استخدام شروط التحقق والـ (DOM Guard clauses) قبل أي عملية تعديل في الـ DOM، لضمان استمرارية تشغيل الموقع وثبات الواجهات:
  ```javascript
  const label = document.getElementById('label-id');
  if (label) {
      label.textContent = data;
  }
  ```
