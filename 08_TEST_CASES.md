# دليل حالات الاختبار والتأكيد (08_TEST_CASES.md)

يحتوي هذا المستند على حالات الاختبار الشاملة (QA Test Cases) للتأكد من عمل كافة أزرار، نماذج، ووظائف بوابة **Spoken English** بنجاح واستقرار.

---

## 🔑 1. اختبار المصادقة والدخول (Authentication Cases)

### حالة اختبار 1.1: تسجيل الدخول الناجح
* **الخطوات:**
  1. الدخول إلى شاشة تسجيل الدخول.
  2. إدخال أي بريد إلكتروني وكلمة مرور (مثل: `ali@spoken.edu` و `password`).
  3. الضغط على زر **Sign In**.
* **النتيجة المتوقعة:**
  * إخفاء شاشة الدخول وعرض لوحة التحكم مباشرة.
  * ظهور رسالة توست خضراء: *"Welcome back, Ali!"*.
  * تشغيل مزامنة قاعدة البيانات في الخلفية وجلب الطلاب.

### حالة اختبار 1.2: تسجيل الخروج
* **الخطوات:**
  1. من داخل لوحة التحكم، الضغط على زر **Logout** (من القائمة الجانبية في الكمبيوتر أو الشريط السفلي في الهاتف).
* **النتيجة المتوقعة:**
  * إخفاء التطبيق فوراً وعرض شاشة تسجيل الدخول.
  * تحديث حالة الجلسة محلياً إلى `false` لمنع الدخول دون تسجيل.
  * ظهور رسالة توست زرقاء: *"Successfully signed out."*.

---

## 🔄 2. اختبار مزامنة البيانات (Data Synchronization Cases)

### حالة اختبار 2.1: جلب البيانات الأولية من NeonDB
* **الخطوات:**
  1. تسجيل الدخول إلى المنصة.
  2. مراقبة بطاقات المستويات الأربعة في لوحة التحكم الرئيسية.
* **النتيجة المتوقعة:**
  * ظهور أرقام الطلاب المسجلين بالخادم تلقائياً (Level 1: 2، بقية المستويات: 1).
  * ظهور آخر سجلات الحضور في بطاقة **Quick Insights** متضمنة النسب المئوية.

---

## 👤 3. اختبار إدارة الطلاب (Student Roster Cases)

### حالة اختبار 3.1: تسجيل طالب جديد بنجاح
* **الخطوات:**
  1. الانتقال إلى تبويب **Students**.
  2. تعبئة النموذج: الاسم `Albus Dumbledore`، رقم القيد `SE202699`، المستوى `Level 1`.
  3. الضغط على زر **Enroll Student**.
* **النتيجة المتوقعة:**
  * إرسال طلب POST إلى دالة `save_student.js` وحفظ الطالب في قاعدة البيانات.
  * إدراج الطالب في جدول الطلاب المعروض بالأسفل فوراً.
  * ظهور توست أخضر: *"Enrolled student..."*.
  * زيادة عدد طلاب المستوى 1 بمقدار طالب واحد في لوحة التحكم.

### حالة اختبار 3.2: منع تكرار رقم القيد (Unique Constraint)
* **الخطوات:**
  1. محاولة تسجيل طالب جديد بنفس رقم قيد طالب مسجل مسبقاً (مثل: `SE202601`).
  2. الضغط على زر **Enroll Student**.
* **النتيجة المتوقعة:**
  * رفض العملية من دالة السيرفر وإعادة خطأ `409 Conflict`.
  * ظهور رسالة توست حمراء: *"Roll number already exists in NeonDB."*.
  * عدم إضافة الطالب للجدول.

### حالة اختبار 3.3: حذف طالب من النظام
* **الخطوات:**
  1. من جدول الطلاب، الضغط على أيقونة سلة المهملات الحمراء بجانب أي طالب.
  2. الموافقة على رسالة التأكيد المتصفح.
* **النتيجة المتوقعة:**
  * إرسال طلب حذف إلى دالة `delete_student.js`.
  * اختفاء الطالب فوراً من الجدول وتحديث الإحصائيات.

---

## 📝 4. اختبار تدوين الحضور (Attendance Recording Cases)

### حالة اختبار 4.1: رصد وحفظ الحضور اليومي
* **الخطوات:**
  1. من لوحة التحكم، الضغط على **Start New Class** بجانب المستوى الأول.
  2. التأكد من ظهور الطلاب المقيدين في المستوى الأول فقط.
  3. تحديد الحالات (Present, Absent, Late).
  4. الضغط على زر **Save Attendance**.
* **النتيجة المتوقعة:**
  * إرسال كشف الحضور إلى دالة `save_attendance.js` وحفظه في NeonDB.
  * الانتقال تلقائياً إلى لوحة التحكم (Dashboard).
  * ظهور الكشف الجديد في قائمة **Quick Insights** فوراً مع حساب النسبة المئوية بدقة.

### حالة اختبار 4.2: التعديل الذكي وتفادي التكرار (UPSERT)
* **الخطوات:**
  1. إعادة فتح كشف الحضور لنفس المستوى ونفس اليوم الحالي.
  2. تغيير حالة أحد الطلاب من غائب إلى حاضر والضغط على **Save Attendance**.
* **النتيجة المتوقعة:**
  * تقوم قاعدة البيانات بتحديث الصف الموجود مسبقاً بدلاً من إنشاء صف جديد.
  * تحديث الإحصائيات والنسب المئوية في التقارير التاريخية فوراً.
