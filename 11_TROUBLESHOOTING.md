# سجل استكشاف الأخطاء وإصلاحها (11_TROUBLESHOOTING.md)

يوثق هذا الملف الأخطاء البرمجية والتقنية التي واجهتنا أثناء تطوير منصة **Spoken English**، مع شرح أسبابها المعمارية وكيف تم التعامل معها وحلها بنجاح.

---

## 1. تجمد الواجهة وظهور خطأ `TypeError: Cannot set properties of null`

### وصف الخطأ (Error Description)
عند محاولة حفظ كشف الحضور والغياب، كان النظام يتجمد على علامة التحميل (Spinner) دون إغلاق النافذة أو إظهار رسالة النجاح (Toast)، مع ظهور خطأ في الكونسول يوضح عدم القدرة على الكتابة لعنصر قيمته `null`.

### التحليل المعماري (Architectural Root Cause)
بما أن الواجهة مبنية كـ Single Page Application (SPA) تعتمد على تبديل التبويبات ديناميكياً، فإن بعض العناصر الرسومية الخاصة بالإحصائيات (مثل لوحات إحصائيات الطلاب والملخصات) قد لا تكون موجودة بالكامل في الـ DOM في لحظة الحفظ. كان الكود يحاول تحديث قيم هذه العناصر بشكل مباشر دون التحقق من وجودها أولاً، مما أدى إلى انهيار المترجم (JavaScript Call Stack) وتوقف تنفيذ بقية الدوال (إخفاء السبينر وإظهار التوست).

### الحل البرمجي (The Solution)
قمنا بتعديل دوال التحديث في ملف `app.js` وإحاطة كل عمليات تحديد عناصر الواجهة بشروط أمان (Conditional Guards):
```javascript
const element = document.getElementById('element-id');
if (element) {
    element.textContent = newValue;
}
```
هذا يضمن استمرار تشغيل الكود وتخطي تحديث العناصر غير المتواجدة بأمان.

---

## 2. تداخل وتلف بيانات الطلاب محلياً (Roster State Corruption & Collisions)

### وصف الخطأ (Error Description)
عند تشغيل الموقع لأول مرة، كان تظهر بيانات قديمة باللغة العربية متداخلة مع البيانات الإنجليزية الجديدة للمستويات الأربعة.

### التحليل المعماري (Architectural Root Cause)
بسبب تشغيل عدة مشاريع تطويرية على نفس الرابط المحلي (`localhost`) ونفس المتصفح، كانت المشاريع تتشارك مساحة التخزين المحلية `LocalStorage` وتستخدم نفس أسماء المفاتيح العامة مثل `students` و `logs` مما أدى لتداخل البيانات (Namespace Collision).

### الحل البرمجي (The Solution)
قمنا بإعادة تهيئة مفاتيح الحفظ ووضع سياق خاص ومستقل باسم المنصة الحالية لتجنب أي تداخل مستقبلي:
* المفتاح الجديد للطلاب: `spoken_english_students`
* المفتاح الجديد للكشوفات: `spoken_english_logs`

---

## 3. فشل تشغيل سكربت الإعداد بسبب خطأ `Cannot find module 'pg'`

### وصف الخطأ (Error Description)
عند تشغيل سكربت تهيئة قاعدة البيانات تلقائياً `node init_db.js` بعد تثبيت مكتبة `pg` بنجاح، انهار التشغيل معطياً خطأ يفيد بعدم إيجاد المكتبة.

### التحليل المعماري (Architectural Root Cause)
يتم تشغيل السكربت من داخل مجلد مؤقت (System Artifacts) يقع خارج مجلد المشروع الرئيسي. تبحث لغة Node.js عن المكتبات في المجلدات الأبوية لملف السكربت الجاري تشغيله وليس في مجلد العمل الحالي للطرفية (Cwd)، وبما أن المجلد المؤقت للنظام لا يحتوي على `node_modules` فشل الاستيراد.

### الحل البرمجي (The Solution)
قمنا بإضافة مسار الـ `node_modules` الخاص بالمشروع الرئيسي برمجياً إلى مصفوفة البحث الخاصة بـ Node.js داخل السكربت قبل استدعاء المكتبة:
```javascript
module.paths.push('c:\\Users\\hk\\Desktop\\Attendance_App_Project\\node_modules');
const { Client } = require('pg');
```

---

## 4. عدم قراءة المتغيرات البيئية من السيرفر المحلي بعد إنشائها

### وصف الخطأ (Error Description)
بعد إنشاء ملف التهيئة الآمن `.env` ووضع رابط `DATABASE_URL` به، كانت الدوال السحابية المحلية تفشل في الاتصال وتظهر أخطاء عدم توفر رابط الاتصال.

### التحليل المعماري (Architectural Root Cause)
يقوم خادم Netlify CLI المحلي (`netlify dev`) بقراءة ملف `.env` ونسخ المتغيرات في ذاكرة النظام المؤقتة **عند الإقلاع فقط**. وبما أن السيرفر كان يعمل بالفعل قبل كتابة الملف، لم يكن على علم بالتحديثات الجديدة.

### الحل البرمجي (The Solution)
تم إيقاف تشغيل خادم Netlify Dev محلياً بالكامل وإعادة تشغيله من جديد، مما أدى لقراءة ملف `.env` بنجاح وتأسيس الاتصال بقاعدة البيانات.
